[build]
  command = "export PRISMA_GENERATE_SKIP_AUTOINSTALL=true && pnpm install --frozen-lockfile && pnpm prisma:generate && pnpm run build"
  publish = ".next"

[build.environment]
  PRISMA_GENERATE_SKIP_AUTOINSTALL = "true"
  NODE_VERSION = "20"
